# src/pages/

Legacy v0.1 page-object tree. New project pages belong under `projects/<project>/pages/`.

Shared page primitives now live in [`harness/runtime/page-core/`](/home/joker/Projects/Euripid/harness/runtime/page-core), while app-specific page objects belong inside a project directory.
