# projects/

Self-contained testing projects. Each project may depend on its own files plus the stable harness entrypoints, but it must not import from sibling projects. `template-project/` is the canonical bootstrap directory and the committed validation target for the rewrite.
