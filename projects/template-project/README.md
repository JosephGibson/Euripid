# template-project/

Canonical bootstrap project for the rewrite. New users should copy this directory to `projects/<new-project-name>/`, rename the project metadata inside `project.config.json`, then edit the copied project's environments, profiles, pages, flows, scenarios, and data in place. This directory is also the committed harness-validation target during the transition away from the legacy v0.1 layout.
